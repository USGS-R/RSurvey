# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: r
sudo: required
dist: trusty
cache: packages

addons:
  apt:
    packages:
      - libgdal-dev
      - libproj-dev
      - tcl8.5-dev
      - tk8.5-dev
      - tk-table

r_binary_packages:
  - rgl
